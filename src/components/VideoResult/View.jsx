
import React from 'react';

import MediaResultDisplay from '../../components/MediaResultDisplay';

const View = ({ item, setVideo }) => (
  <MediaResultDisplay item={item} setMediaItem={setVideo} />
);

export default View;
